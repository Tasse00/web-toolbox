import { AppConfig } from "toolbox-framework";
import App from "./App";

export const WebsocketAppConfig: AppConfig = {
  appId: "websocket",
  component: App,
  title: "Websocket",
  defaultProps: {},
  suggestSize: [800, 600],
};
